<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.clinc8686.hochschul_crawler">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/hochschulcrawlerlogoicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hochschulcrawlerlogoicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="de.clinc8686.hochschul_crawler.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service android:name=".Crawler_Service"
            android:label="Crawler"
            android:enabled="true"
            android:permission="android.permission.BIND_JOB_SERVICE"
            tools:ignore="MissingClass,WrongManifestParent" />
    </application>
</manifest>